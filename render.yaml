services:
  - type: web
    name: richbelspa-aestheticscentre
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:application
    autoDeploy: true
    envVars:
      - key: FLASK_ENV
        value: production
